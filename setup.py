from setuptools import find_packages, setup

setup(name='kareem',
      version='0.1',
      author='kareem alkoul',
      author_email='ss@example.com',
      license='MIT',
      install_requires=[
          'numpy',
          "multipledispatch"
      ],
      )
